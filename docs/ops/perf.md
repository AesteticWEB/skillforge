# Performance

Budgets (production build):

- Initial JS bundle: < 300KB gzip.
- LCP target: p75 < 2.5s.
- INP target: p75 < 200ms.

Checks:

- `npm run build` and inspect `dist/skillforge`.
- Use Chrome DevTools Performance for the `/skills` and `/analytics` pages.
