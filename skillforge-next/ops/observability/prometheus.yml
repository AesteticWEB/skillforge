global:
  scrape_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: "skillforge-backend"
    metrics_path: /api/metrics
    static_configs:
      - targets: ["host.docker.internal:3002"]
